{
  "name": "casepilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @casepilot/core && npm run build -w @casepilot/providers && npm run build -w @casepilot/mcp && npm run build -w @casepilot/server && npm run build -w @casepilot/cli && npm run build -w @casepilot/dashboard",
    "test": "npm run test --workspaces --if-present"
  }
}
